async function cargarCortes(){try{const respuesta=await fetch('/api/restaurant/settlement',{headers:{Authorization:'Bearer '+obtenerToken()}}),datos=await respuesta.json();if(!respuesta.ok)throw new Error(datos.error||'No fue posible cargar los cortes');const box=document.getElementById('cortesRestaurante');if(!box)return;box.innerHTML=datos.days.length?datos.days.map(d=>`<article style="background:#fff;border:1px solid #eadfd6;border-radius:12px;padding:13px"><strong>${d.day}</strong><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:8px;margin-top:9px"><span>Pedidos: ${d.orders_count}</span><span>Ventas: $${Number(d.sales).toFixed(2)}</span><span>Comisión: $${Number(d.commission).toFixed(2)}</span><span>Efectivo: $${Number(d.cash_orders).toFixed(2)}</span><span>Digitales: $${Number(d.digital_orders).toFixed(2)}</span><strong>A recibir: $${Number(d.restaurant_due).toFixed(2)}</strong><span>Cancelaciones: ${d.cancellations}</span></div></article>`).join(''):'Todavía no hay movimientos.';document.getElementById('porcentajeComision').textContent=`Comisión configurada actualmente: ${datos.commissionPercent}%`;}catch(e){const box=document.getElementById('cortesRestaurante');if(box)box.textContent=e.message}}
cargarCortes();

